/* Source and licensing information for the line(s) below can be found at http://hackathondigitalumni.docker.localhost:8000/modules/contrib/permissions_filter/js/permissions_filter.js. */
(function($,Drupal,debounce){'use strict';Drupal.behaviors.permissionTableFilterByText={attach:function(context,settings){var $input=$('input.table-filter-text').once('table-filter-text'),$table=$($input.attr('data-table')),$rowsAndDetails,$rows,$details,searching=false
function hidePackageDetails(index,element){var $packDetails=$(element),$visibleRows=$packDetails.find('tbody tr:visible');$packDetails.toggle($visibleRows.length>0)}
function filterPermissionList(e){var query=$(e.target).val(),re=new RegExp('\\b'+query,'i')
function showPermissionRow(index,row){var $row=$(row),$sources=$row.find('.table-filter-text-source, .module, .permission'),textMatch=$sources.text().search(re)!==-1;$row.closest('tr').toggle(textMatch)};$rowsAndDetails.show();if(query.length>=2){searching=true;$rows.each(showPermissionRow);$details.not('[open]').attr('data-drupal-system-state','forced-open');$details.attr('open',true).each(hidePackageDetails);Drupal.announce(Drupal.t('!permissions permissions are available in the modified list.',{'!permissions':$rowsAndDetails.find('tbody tr:visible').length}))}else if(searching){searching=false;$rowsAndDetails.show();$details.filter('[data-drupal-system-state="forced-open"]').removeAttr('data-drupal-system-state').attr('open',false)}}
function preventEnterKey(event){if(event.which===13){event.preventDefault();event.stopPropagation()}};if($table.length){$rowsAndDetails=$table.find('tr, details');$rows=$table.find('tbody tr');$details=$rowsAndDetails.filter('.js-permissions');$input.on({keyup:debounce(filterPermissionList,200),keydown:preventEnterKey})}}}}(jQuery,Drupal,Drupal.debounce))
/* Source and licensing information for the above line(s) can be found at http://hackathondigitalumni.docker.localhost:8000/modules/contrib/permissions_filter/js/permissions_filter.js. */