version: "3"

services:
  php:
    volumes:
## For macOS users (https://wodby.com/docs/stacks/drupal/local#docker-for-mac)
#      - ./:/var/www/html:cached # User-guided caching
      - hackathondigitalumni_sync:/var/www/html # Docker-sync

  nginx:
    volumes:
# For macOS users (https://wodby.com/docs/stacks/drupal/local#docker-for-mac)
#      - ./:/var/www/html:cached # User-guided caching
      - hackathondigitalumni_sync:/var/www/html # Docker-sync

#  apache:
#    volumes:
## For macOS users (https://wodby.com/docs/stacks/drupal/local#docker-for-mac)
##      - ./:/var/www/html:cached # User-guided caching
##      - digitalumni_sync:/var/www/html # Docker-sync

volumes:
## Docker-sync for macOS users
  digitalumni_sync:
    external: true
